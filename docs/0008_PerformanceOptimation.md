# [Performance Optimation"${ROOTLINK}"
- SIMD"${ROOTLINK}"
- Loop Optimation"${ROOTLINK}"
- Memory"${ROOTLINK}"
  - Align"${ROOTLINK}"
  - CacheLine"${ROOTLINK}"
  - Cache->Register"${ROOTLINK}"
- LIBS&Tools"${ROOTLINK}"
  - OpenMP"${ROOTLINK}"
  - OpenCL"${ROOTLINK}"
  - SYCL"${ROOTLINK}"
  - DPCPP"${ROOTLINK}"
  - OpenMPI"${ROOTLINK}"
  - TBB"${ROOTLINK}"
- DebugTools"${ROOTLINK}"
  - IntelVtune"${ROOTLINK}"
  - IntelAdvisor"${ROOTLINK}"
